@echo off
REM Double-click this to start Huaban (app + OpenCode bridge + NVIDIA speech server).
REM See scripts/start.ps1 for what each window does.
powershell -ExecutionPolicy Bypass -NoProfile -File "%~dp0start.ps1"
